# 소비자 동선 감리 · 2026-09-26

## 결론

소비자 화면은 1차 상담 MVP에 맞췄다. 홈 → 무료 상담 → 입력 확인 → 접수로 연결한다.
그러나 관리자 인증과 DB 정책을 검증하지 않았으므로 실고객 유입 준비 완료는 아니다.

## 발견 및 수정

| 문제 | 수정 |
| --- | --- |
| 홈에서 미구현 PDF/AI 서비스와 정확도 수치 홍보 | 실제 제공 중인 상담 신청을 주 행동으로 변경 |
| 모르는 평수·업종·계획에서 진행 중단 | 미정 선택, 경험/메모 선택 입력, 도움 문구 |
| 뒤로 가면 입력 소실 / 다른 단계 덮어쓰기 | 공유 상태 저장·복원, 현재 답변만 업데이트 |
| 접수 DB error를 무시하고 완료 표시 | error 검사, 실패 안내·재시도, 15초 시간 제한 |
| 연락처 오류 / 중복 클릭 | 전화번호 검사, 명시적 레이블, 요청 중 잠금 |
| 계산 step5를 직접 열면 빈 조건 결과 | 선행 단계 가드 |
| 이메일 넣으면 보내지 않은 PDF를 발송했다고 표시 | 가짜 발송 UI 제거, 참고 금액과 상담 연결 |
| 계산 후 상담에서 다시 정보 입력 | 지역/공간/면적과 공사 메모 전달 |
| 미구현 로그인/회원가입이 비밀번호 입력 요구 | 회원가입 없이 상담 가능 안내 |
| 사이트맵의 다른 도메인·없는 경로 | 확인된 기본 배포 URL과 /consult로 통일 |
| 한글 블로그 주소에서 404 | URL 디코딩·유니코드 정규화 및 회귀 테스트 |
| 블로그 잘못된 표/목록 구조 | table/thead/tbody와 ul로 묶음 |

## 재현 가능한 검증

- npm run test:forms: 12개 통과.
- npx tsc --noEmit, 변경 파일 ESLint, npm run build: 통과.
- 브라우저 320px/390px/1280px: 홈 CTA, 미정 상담, 면적 상한, 연락처 오류, 실패 후 입력 보존, 15초 시간초과 복구, 성공 후 초기화.
- 계산: 알려진 업종의 세부 업종 요구, unknown 진행, 25평/도배/스탠다드 결과, 이전 조건 보존, 상담 전달.
- 접수 테스트는 아래 로컬 fixture만 사용. 실제 고객 정보·운영 DB 조회/삽입 없음.
- 전체 lint의 기존 잔여: admin 2개(set-state-in-effect), partner/leads 1개(set-state-in-effect)와 미사용 import 경고, page.v1 백업 2개(no-unescaped-entities). 소비자 변경 경로의 오류와 구분한다.

### 로컬 접수 테스트 (PowerShell, 별도 터미널 2개)

터미널 A: node tests/mock-supabase.mjs

터미널 B:

```powershell
$env:NEXT_PUBLIC_SUPABASE_URL='http://127.0.0.1:4333'
$env:NEXT_PUBLIC_SUPABASE_ANON_KEY='local-qa-only-not-a-real-key'
npm run dev -- -p 3004
```

http://localhost:3004 에서 신청. 이름 오류테스트는 503, 시간초과테스트는 응답 지연, 나머지는 201을 반환한다.
실제 연락처 대신 테스트 값 사용. 서버는 데이터를 저장하지 않는다. 위 테스트 환경변수는 배포에 절대 사용하지 않는다.
운영 빌드 전 테스트 터미널을 닫거나 해당 환경변수를 제거한다.

## 유입 전 운영/보안 차단 항목

1. 관리자 클라이언트 하드코딩 암호 및 sessionStorage 플래그는 인증 경계가 아니다. 서버 인증과 역할 권한이 필요하다.
2. 기존 문서는 leads/consultations RLS 비활성화라고 명시한다. 실제 DB grants/policies는 미검증이다. 공개 키로 개인정보 조회/수정/삭제가 가능한지 **데이터를 노출하지 않는 방식으로** 관리자가 감사해야 한다. robots 제외는 보안 조치가 아니다.
3. consultations 관리 UI와 접수 알림이 없다. 새 신청을 실제로 보고 연락할 운영 경로를 먼저 확보해야 한다.
4. nullable 면적·미정 enum 값은 저장 SQL상 허용되나 배포된 DB 제약조건은 별도 확인 필요.
5. 동의 문구의 보유/파기 약속에 맞는 실제 처리, 운영 주체·문의 채널, 동의 증빙은 별도 확정 필요. 법률 검수 완료를 의미하지 않는다.
6. 서버 검증·속도 제한·봇 방어 미구현. 클라이언트 잠금은 동일 화면 더블클릭만 막는다. 서버 저장 후 응답 유실 시 재시도로 중복될 수 있어 idempotency 보강 필요.
7. 블로그의 공사별 단가/시공 지식은 이번 UX 감리의 사실 검증 범위가 아니다. 광고·견적 근거로 쓰기 전 전문가 검수 필요.

실제 DB 권한 변경·관리자 계정 생성·고객 데이터 삭제·운영 테스트 신청은 이번 작업에서 하지 않았다.
